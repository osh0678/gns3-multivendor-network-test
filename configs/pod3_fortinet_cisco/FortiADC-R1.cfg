! Pod: Fortinet Router & Cisco Switch
! Device: FortiADC-R1 (router)
! Generated by GNS3 Multivendor Network Test
!==================================================

! === Fortinet - FortiADC-R1 ===
config system global
    set hostname FortiADC-R1
end
!
config system interface
    edit "port2"
        set vdom "root"
        set mode static
        set type physical
        set allowaccess ping https ssh
        set status up
    next
end
!
config system interface
    edit "vlan50"
        set vdom "root"
        set ip 10.3.50.1 255.255.255.0
        set allowaccess ping
        set type vlan
        set vlanid 50
        set interface "port2"
        set status up
    next
end
!
config system interface
    edit "port3"
        set vdom "root"
        set mode static
        set type physical
        set allowaccess ping https ssh
        set status up
    next
end
!
config system interface
    edit "vlan60"
        set vdom "root"
        set ip 10.3.60.1 255.255.255.0
        set allowaccess ping
        set type vlan
        set vlanid 60
        set interface "port3"
        set status up
    next
end
!
config router static
    edit 0
        set dst 0.0.0.0 0.0.0.0
        set device "port1"
    next
end
